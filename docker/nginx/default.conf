server {
    listen 80;
    server_name _;
    root /var/www/html/public;
    index index.php;

    # Securite minimale (les headers complets sont aussi poses cote PHP)
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    client_max_body_size 4M;

    # Toutes les URL non statiques sont reroutees vers index.php (front-controller Slim).
    location / {
        try_files $uri /index.php$is_args$args;
    }

    location ~ \.php$ {
        # Garde-fou : on ne forwarde a FastCGI que les fichiers .php qui existent
        # reellement, pour eviter d'executer du code via des chemins inattendus.
        try_files $uri =404;

        fastcgi_pass app:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
    }

    # On bloque l'acces direct aux dossiers internes au cas ou ils seraient exposes.
    location ~ /(app|config|migrations|var|bin|tests)/ {
        deny all;
        return 403;
    }
}
